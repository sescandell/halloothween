# Configuration RPI pour Streamer
# Copiez ce fichier vers .env et ajustez les valeurs

# URL de votre Service de streaming de la photo (pour DL QRCode - après déploiement)
STREAMER_URL=https://your-app.azurewebsites.net

# Secret partagé (même valeur que dans votre streamer)
STREAMER_SHARED_SECRET=your-shared-secret-here

# ID unique de ce RPI
RPI_ID=rpi-001

# Activer/désactiver Streamer (true/false)
STREAMER_ENABLED=true

# ============================================
# CAMERA DRIVER CONFIGURATION
# ============================================

# Camera Driver Selection
# Options: "auto" | "webcam" | "gphoto2" | "gphoto2-legacy"
# 
# - auto: Auto-detect based on platform (default)
#   * Windows → webcam
#   * Linux → gphoto2 (with fallback to gphoto2-legacy)
# 
# - webcam: System webcam via node-webcam
#   * Works on Windows and Linux
#   * Good for development
# 
# - gphoto2: Modern driver via @photobot/gphoto2-camera (RECOMMENDED for production)
#   * Actively maintained (2025)
#   * Koffi FFI - no native compilation
#   * Linux only
# 
# - gphoto2-legacy: Old driver (deprecated, kept for fallback)
#   * Unmaintained since 2022
#   * Requires native compilation
#   * May have compatibility issues
#
CAMERA_DRIVER=auto

# Mode de capture avec pause du flux vidéo
# - false: Capture directe sans pause (défaut, pour production)
# - true: Pause le flux vidéo avant capture (utile pour dev/debugging)
# Compatible avec tous les drivers
PAUSE_STREAM_ON_CAPTURE=false

# ============================================
# PRINTER CONFIGURATION
# ============================================

# Enable/disable printer (true/false)
PRINTER_ENABLED=false

# Printer name (Raspberry Pi/Linux only)
# Windows: Always uses SIMULATION mode (saves to /print folder)
# Linux: "DNP_QW410" (configure via CUPS) or "__SIMULATION__" for testing
PRINTER_NAME=DNP_QW410

# Print mode: auto (direct system print)
PRINTER_MODE=auto